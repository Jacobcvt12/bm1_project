create.data <- function(params,
                        dir,
                        distribution=rnorm, 
                        mix.prob=c(0.5, 0.5), 
                        obs=100)
{
    k <- length(mix.prob)

}

